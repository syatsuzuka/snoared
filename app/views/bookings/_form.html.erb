<%= simple_form_for [@gear, @booking] do |f| %>
  <%= f.input :start_date, html5: true %>
  <%= f.input :end_date, html5: true %>
  <%= f.submit class:'btn btn-danger mb-3 mt-3 text-center'%>
<% end %>
