<div class="cards">
  <% @gears.each do |gear| %>
    <div class="cards-index d-flex flex-column">
      <img src=<%=gear.img_url%>  />
        <h2><%= gear.title %></h2>
        <h2><strong><%= gear.address %></strong></h2>
        <p><%= gear.description.split.first(29).join(" ") + "..." %></p>
        <h2 class="mt-auto"><%="¥#{gear.price.to_i}" %></h2>
    </div>
  <% end %>
</div>
