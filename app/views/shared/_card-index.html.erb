<div class="cards">
  <% @gears.each do |gear| %>
    <div class="cards-index">
      <img src="https://raw.githubusercontent.com/lewagon/fullstack-images/master/uikit/greece.jpg"  />
      <h2><%= gear.title %></h2>
      <h2><%= gear.address %></h2>
      <p><%= gear.description.split.first(29).join(" ") + "..." %></p>
      <h2><%="¥#{gear.price.to_i}" %></h2>
    </div>
  <% end %>
</div>
