
 <%= form_tag gears_path, method: :get, class: 'd-flex my-5' do %>
    <%= text_field_tag :q,
      params[:q],
      class: "form-control",
      placeholder: "Find a gear"
    %>
    <%= submit_tag "Search", class: "btn btn-danger text-white ms-3" %>
  <% end %>
<div class="container-fluid d-flex flex-row-reverse">
<div class="dropdown">
  <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton1" data-bs-toggle="dropdown" aria-expanded="false">
    Dropdown button
  </button>
  <ul class="dropdown-menu" aria-labelledby="dropdownMenuButton1">
    <li><a class="dropdown-item" href="#">Price low</a></li>
    <li><a class="dropdown-item" href="#">Creation date</a></li>
  </ul>
</div>
</div>

<div class="container-fluid col-11 d-flex flex-direction-column justify-content-be">

  <div class="col-8 ">
    <%= render 'shared/card-index'%>
  </div>
  <div class="col-4 ">

  <div style="width: 100%; height: 100vh; margin-left: 25px; position:sticky; top: 0;"

  data-controller="mapbox"
  data-mapbox-markers-value="<%= @markers.to_json %>"
  data-mapbox-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>"></div>
  </div>
</div>
